<attention>
The session directory contains exactly 1 direct-child git repo: `{{relativeRepoRoot}}`.
It may be the relevant project; this hint does not establish the active repository.
Before claiming work missing, destroyed, or absent at parent cwd, check `{{relativeRepoRoot}}/`.
</attention>
